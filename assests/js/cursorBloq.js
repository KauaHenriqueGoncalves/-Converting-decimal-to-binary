//Sinalizar que o bloco de saída de dados está bloqueado

// Adicione um ouvinte de evento para o evento 'mouseenter'
numBinary.addEventListener('mouseenter', () => {
    // Altere o estilo do cursor para o cursor desejado
    numBinary.style.cursor = 'not-allowed';
});